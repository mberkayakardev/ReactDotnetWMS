namespace Core.Utilities.Helper
{
    public static class MVCMenuHelper
    {
        //private static List<ListAppMVCMenusDto> BuildMenuHierarchy(List<ListAppMVCMenusDto> items, int? parentId)
        //{
        //    return items
        //        .Where(i => i.ParentId == parentId)
        //        .Select(i => new MenuItem
        //        {
        //            Id = i.Id,
        //            Title = i.Title,
        //            Url = i.Url,
        //            ParentId = i.ParentId,
        //            SubItems = BuildMenuHierarchy(items, i.Id)
        //        })
        //        .ToList();
        //}
    }
}
